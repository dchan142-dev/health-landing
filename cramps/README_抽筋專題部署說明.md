# Health Decoder 半夜抽筋專題 · 獨立交付套件說明 (2026年9月最新版)

## 一、 套件內容
1. ：**半夜抽筋專題獨立落地頁**
   - 包含 13 大標準 Bento Grid 模組（WCAG 2.1 AA 無障礙標準）。
   - 完整五大成因對稱 7 分自測問卷（力學、血管、神經、代謝、西藥審查），含急症紅旗篩查。
   - 全域雙向嚴格互斥引擎（點選具體體徵自動取消「以上皆無」，點選「以上皆無」自動清空所有選項）。
   - 多層仲裁演算法（年齡加權 1.2 倍 ✕ 3 分核心硬指標優先 ✕ 臨床緊急優先序唯一仲裁：5 > 3 > 2 > 4 > 1）。
   - 15 支 Vimeo 官方短影音全面動態嵌入（Block D/E/F 標題下方 16:9 自適應容器）。
   - Block H 結算區全面升級：
     - 加入「滿 HKD 1,200 尊尚會員特別優惠」自動門檻判定、折數計算與即時湊單提示橫幅；
     - 加入 3 大主流付款方式單選：🔵 FPS 轉數快 (預設)、🔴 PayMe、🔵 AlipayHK；
     - 雙軌下單引擎：非同步同步寫入 Google Apps Script 雲端數據庫 ＋ 自動組裝 WhatsApp 完整訂單跳轉。
   - 10 題原汁原味 Q&A 手風琴專區（Block M）。
   - 右下角 11 級狀態機電梯平滑滾動按鈕（Block K）。
   - 全域模態彈窗（#globalModal，深色背景 ＋ 純白圖框 ＋ 縮圖輪播）。

2. ：**全站統一訂單與自測 Webhook 路由器**
   - 支援香港時區 (GMT+8)，雙試算表自動路由寫入（全站統一訂單總表與用戶自測數據庫）。

## 二、 部署步驟 (GitHub ➔ Vercel)
1. **替換或新增檔案**：
   - 將本壓縮包解壓後的  放置於您本地 Git 倉庫的  目錄下（覆蓋舊有檔案）。
2. **圖庫確認**：
   - 請確保專案根目錄維持標準共用圖庫路徑： 與 。
3. **GitHub Desktop 發布**：
   - 開啟 GitHub Desktop，確認變更，輸入 Commit 訊息（例如：）。
   - 點擊 **Push origin**，Vercel 將在 15 秒內全自動完成部署上線！
